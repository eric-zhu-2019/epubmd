[package]
name = "epubmd-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
regex = "1"
roxmltree = "0.20"
zip = { version = "2", default-features = false, features = ["deflate"] }

[dev-dependencies]
tempfile = "3"

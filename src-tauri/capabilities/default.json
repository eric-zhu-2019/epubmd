{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for opening epubmd zip archives.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}

{
  "name": "epubmd-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "test:frontend": "rm -rf dist-test && tsc src/cssScope.ts src/cssScope.test.ts src/readerNavigation.ts src/readerNavigation.test.ts --outDir dist-test --module ES2022 --target ES2022 --moduleResolution Bundler --skipLibCheck --strict --noEmit false && node dist-test/cssScope.test.js && node dist-test/readerNavigation.test.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "dompurify": "^3.0.0",
    "marked": "^16.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}

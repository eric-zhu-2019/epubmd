{
  "name": "goosereader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "tauri:ios": "script/tauri_ios_xcode_env.sh",
    "test:frontend": "rm -rf dist-test && tsc src/cssScope.ts src/cssScope.test.ts src/readerNavigation.ts src/readerNavigation.test.ts src/chapterSections.ts src/chapterSections.test.ts src/tocStructure.ts src/tocStructure.test.ts --outDir dist-test --module ES2022 --target ES2022 --moduleResolution Bundler --skipLibCheck --strict --noEmit false && node dist-test/cssScope.test.js && node dist-test/readerNavigation.test.js && node dist-test/chapterSections.test.js && node dist-test/tocStructure.test.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "dompurify": "^3.0.0",
    "marked": "^16.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}
